# Reviewer 终审结果模板

```markdown
| 等级 | 问题 | 位置 | 违反规则 | 处理建议 | 是否已修复 |
|---|---|---|---|---|---|
| error/warning/info | {问题描述} | {章节/行/条目} | {规则名} | {删除/替换/降级/补证据} | 是/否 |
```

判定：

- 存在未修复 `error`：报告状态必须为阶段性调研稿。
- 存在 `warning`：可以正式报告，但必须在待核实线索或说明中提醒用户。
- 只有 `info` 或无问题：可标为正式报告。
