# 事实准入表模板

```markdown
| 单位 | 原子事实 | 链接状态 | 页面相关性 | 准入结果 | 可用于章节 | 限制说明 |
|---|---|---|---|---|---|---|
| {单位名称} | {原子事实} | 200 | 支持 | 准入/待核实/剔除 | 基本介绍/职责/机构/负责人/动态/来源汇总 | |
```

准入规则：

- `准入`：可进入正文。
- `待核实`：只能进入“待核实线索”。
- `剔除`：不得进入报告。

任何 `待核实` 或 `剔除` 事实不得在正文中写成确定表述。
